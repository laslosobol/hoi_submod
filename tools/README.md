# Tools

Place small validation or export scripts here as the project grows.

Useful future checks:

- Verify descriptors have matching `name`, `version`, `supported_version`, `tags`, and `dependencies`.
- Scan scripted files for mixed tabs/spaces or missing braces.
- Package `mod/HoISubmod` into a release archive under `dist/`.
